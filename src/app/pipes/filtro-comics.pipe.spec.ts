import { FiltroComicsPipe } from './filtro-comics.pipe';

describe('FiltroComicsPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroComicsPipe();
    expect(pipe).toBeTruthy();
  });
});
