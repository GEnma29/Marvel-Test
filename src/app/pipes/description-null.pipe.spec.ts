import { DescriptionNullPipe } from './description-null.pipe';

describe('DescriptionNullPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionNullPipe();
    expect(pipe).toBeTruthy();
  });
});
