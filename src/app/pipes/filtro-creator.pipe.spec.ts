import { FiltroCreatorPipe } from './filtro-creator.pipe';

describe('FiltroCreatorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCreatorPipe();
    expect(pipe).toBeTruthy();
  });
});
